# -----------------------------
# Core Backend
# -----------------------------
fastapi
uvicorn[standard]
pydantic
python-dotenv

# -----------------------------
# LLM Providers (via unified manager)
# -----------------------------

# OpenAI (also used for DeepSeek API via custom base URL)
openai>=1.0.0

# Grok by xAI
xai-sdk

# Anthropic (Claude)
anthropic

# Google Gemini
google-generativeai

# Local LLM (with PyTorch)
torch
# Ensure compatible tokenizers for CrewAI/Transformers stack
tokenizers>=0.21,<0.22
# Upgrade transformers to a version compatible with tokenizers >=0.21
transformers>=4.45.0
accelerate>=1.0.0

# -----------------------------
# CrewAI Agents (align with modern langchain & packaging)
# -----------------------------
packaging>=25.0
langchain-core>=0.3.0
langchain>=0.3.0
crewai>=0.54.0
crewai[tools]>=0.54.0

# -----------------------------
# Data + Finance
# -----------------------------
numpy
pandas
yfinance
requests
httpx

# MarketWatch price scraper
beautifulsoup4
lxml

# -----------------------------
# Streamlit Frontend
# -----------------------------
streamlit
plotly
